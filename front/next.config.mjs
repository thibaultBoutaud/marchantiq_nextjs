// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // pas de output: 'export'
  images: {
    // tu peux même laisser vide, ou supprimer complètement "images"
  },
};

export default nextConfig;
